FROM node:20-alpine
WORKDIR /app

# Install curl for health checks and libreoffice for PDF conversion
RUN apk add --no-cache curl libreoffice

COPY package*.json ./
RUN npm install

# Copy source code (will be overridden by volume mounts in development)
COPY src ./src

EXPOSE 3001
CMD ["npm", "run", "dev"]
